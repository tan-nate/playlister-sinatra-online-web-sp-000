<%# binding.pry %>
<% if flash.has?(:message) %>
  <p><%= flash[:message] %></p>
<% end %>

<h1><%= @song.name %></h1>
<h2>by <%= @song.artist.name %></h2>
<br>
<h2>Genres:</h2>
<ul>
  <% @song.genres.each do |genre| %>
    <li><%= genre.name %></li>
  <% end %>
</ul>
